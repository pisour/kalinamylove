# My Love

A small static apology page built for GitHub Pages.

## Launch on GitHub Pages

1. Push this folder to a GitHub repository.
2. In GitHub, open `Settings` -> `Pages`.
3. Set the source to the `main` branch and `/root`.
4. Save, then open the Pages URL GitHub gives you.

The page has no build step or package dependencies. `index.html` is the entry point.
